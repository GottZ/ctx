package overview

// LockKeyForScopes exposes lockKeyForScopes to the external integration test
// package (standard export_test idiom — test binary only, never shipped).
var LockKeyForScopes = lockKeyForScopes
