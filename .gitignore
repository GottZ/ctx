# Ignore all dotfiles/dotdirs (.env, .n8n/, .claude/, .project/, .eval-baseline.json, ...)
.*
!.gitignore
!.gitmodules
!.env.example
!.project
# CI workflow assets (workflows/ were force-added before this exception;
# scripts/ + budget json arrived with wave PV8):
!.github

# Local overrides
docker-compose.override.yml

# Data & backups
db/
backups/
backup-*.sql

# Build artifacts
go/ctx
go/dist/

# Frontend (Svelte 5 + Vite, bun)
go/web/node_modules/
go/web/playwright-report/
go/web/e2e/perf/.lhci/
go/web/.lighthouseci/
go/web/dist/*
!go/web/dist/.gitkeep
!.gitkeep
!go/.dockerignore

# Misc
*.swp
*.swo
*~
Thumbs.db

# Python bytecode (hermes plugin tests)
__pycache__/
